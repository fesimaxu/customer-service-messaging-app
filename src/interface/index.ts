// message interface
export interface IMessagePortal {
    userId: string;
    message: string;
    timestamp: string
}

// agent interface
export interface IAgentPortal {
    agentId: string;
    username: string;
    email: string;
    employmentYear: string;
}